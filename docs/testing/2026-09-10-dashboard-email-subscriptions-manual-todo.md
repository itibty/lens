# 看板邮件订阅人工验收 TODO

日期：2026-09-10

用途：记录必须依赖真实 SMTP、Chromium、邮件客户端或人工视觉判断的验收项。自动化检查及其结果见实施计划。

## 验收前准备

- [ ] 现有库执行 `backend/db/upgrade-20260910-dashboard-subscriptions.sql`；全新库只执行 `backend/db/schema.sql`。
- [ ] 执行 `mvn exec:java -Dexec.mainClass=com.microsoft.playwright.CLI -Dexec.args="install chromium"`；或者安装 Chrome 后设置 `LENS_BROWSER_CHANNEL=chrome`。
- [ ] 构建前端并确认 `LENS_PUBLIC_BASE_URL` 能直接打开 Lens SPA 及看板路由。
- [ ] 配置专用测试收件箱、`LENS_MAIL_*`、`LENS_MAIL_FROM` 和 `LENS_SUBSCRIPTION_ENABLED=true`。
- [ ] 在用户管理中给测试用户绑定上述邮箱，确认不会发往真实业务收件人。

## 交互与数据

- [ ] 无邮箱用户可打开订阅抽屉，能看到提示，但不能新建或测试发送。
- [ ] 同一看板分别创建“每天”、“工作日”、“每周”、“每月”订阅，互不覆盖。
- [ ] 编辑订阅后名称、时间、星期/日期和下次发送时间均更新。
- [ ] 停用后不再被调度；重新启用后从当前时间计算下一次发送。
- [ ] 删除后订阅从列表消失，其他用户无法查看或操作该订阅。

## 截图和邮件

- [x] 正常 SMTP 链路下，点击“测试发送”后收到一封邮件，执行状态最终变为成功。
- [ ] 邮件主题、看板名、生成时间和链接正确；链接不含 Token，未登录时要求登录。
- [ ] 分别用主流浏览器邮箱、Outlook 和手机邮件客户端打开，CID PNG 可见，即使图片被拦截也仍有可点链接。
- [ ] 选择一个长看板，确认截图覆盖完整滚动区，不包含工具栏、抽屉、加载动画或裁切错位。
- [ ] 包含延迟查询、表格、透视表、图表和中文字体的看板截图内容完整。
- [ ] 收件人和截图数据均属于订阅所有者，不会使用触发调度的管理员权限。

## 调度、权限与失败恢复

- [ ] 将订阅的 `next_fire_at` 改为过去时间，确认 30 秒内只生成一条运行记录和一封邮件。
- [ ] 将 `next_fire_at` 改为数天前，确认只补发一次，新的下次发送时间直接落在当前时间之后，不连续追补历史周期。
- [ ] 任务执行前撤销用户的看板授权，确认不发邮件、记录失败原因并自动停用订阅。
- [ ] 禁用用户或看板，得到与撤权相同的安全结果。
- [ ] 在截图期间重启应用，确认不会重复入队，超过恢复窗口后原 `RUNNING` 记录被标记为失败。
- [ ] 模拟“SMTP 已接收邮件但 Lens 未收到成功响应”，记录重试可能导致重复邮件的供应商行为。
- [ ] 依次模拟 SMTP 拒绝、SMTP 超时、Chromium 缺失、页面超时和 PNG 超限，确认应用不崩溃，任务最多尝试两次并保留脱敏错误。
- [ ] 关闭 `LENS_SUBSCRIPTION_ENABLED`，确认 Lens 其他功能正常，订阅 CRUD 可用，测试发送返回明确的“尚未启用”错误。

## 验收记录

- 2026-09-10 本机首次试跑：复用系统 Chrome 成功加载看板、查询数据并进入邮件发送；原临时 SMTP 密码认证失败。
- 2026-09-10 本机复验：使用 `data-work` 已有加密配置解密出的凭据（明文未写入 Lens 配置或文档），通过 163 SMTP 端到端发送成功。运行记录 `2098000075622834178` 状态为 `SUCCESS`，一次尝试，截图大小 318434 字节；收件人随后确认 QQ 邮箱已收到。
- 2026-09-10 首封邮件反馈部分 Chart 效果未完成。定位为看板截图等待出数后未像卡片截图一样结束 VChart 入场动画；已增加所有图表最终帧重绘和双动画帧等待，回归测试覆盖通知范围和等待时序。修复版运行记录 `2098002207310725121` 状态为 `SUCCESS`，一次尝试，截图大小 340523 字节。
- 2026-09-10 按反馈将邮件改为宽幅数据简报样式：外围留白缩小为 12×8 像素、内容宽度 1600 像素、深色标题区、截图全宽直角无边框。新版运行记录 `2098002743204356098` 状态为 `SUCCESS`，一次尝试，截图大小 341608 字节；等待收件人确认新版视觉效果。
- 验收人：
- 验收环境/版本：
- SMTP 供应商与邮件客户端：
- 未通过项及问题链接：
- 结论：
